[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hoverpreview-tools"
version = "0.1.0"
description = "Hover preview export tooling for UKGeo raster datasets."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "ukgeo-tools",
  "numpy",
  "Pillow",
  "typer",
  "rich",
]

[project.optional-dependencies]
test = ["pytest"]
gpu = ["cupy-cuda12x[ctk]"]

[project.scripts]
hoverpreview = "hoverpreview_tools.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["src/hoverpreview_tools"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "../ukgeo-tools/src"]
